name: Make build CI

on:
  push:
    branches:
      [ main ]
  pull_request:
    branches:
      [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: make
        uses: actions/checkout@v2
        run: apt install -y make gcc nasm; make
